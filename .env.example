# Copy to .env (never commit it) or set these as GitHub Actions secrets.
# Only needed for real WhatsApp alerts; --dry-run and --demo work without them.
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_FROM=
WHATSAPP_TO=
